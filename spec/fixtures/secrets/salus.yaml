scanner_configs:
  Trufflehog:
    exclude_files: # List of file paths to ignore
      - url.txt
      - logins.txt
    only-verified: false  # Only output verified results.
                          # true by default