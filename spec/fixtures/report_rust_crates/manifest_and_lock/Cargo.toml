[package]
name = "foo"
version = "0.1.0"
authors = ["joshua-ostrom <joshua.ostrom@coinbase.com>"]
edition = "2018"

# No dependencies so we KNOW if it's loading them from the lock or not
[dependencies]
