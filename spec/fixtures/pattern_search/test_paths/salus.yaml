scanner_configs:
  PatternSearch:
    matches:
      - regex: hello
        forbidden: true
        exclude_filepaths:
          - file.txt
          - subdir/file.txt
        exclude_extension:
          - yaml
