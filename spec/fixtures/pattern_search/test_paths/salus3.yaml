scanner_configs:
  PatternSearch:
    exclude_filepaths:
      - file.txt
      - subdir/file.txt
    matches:
      - regex: hello
        forbidden: true
        exclude_extension:
          - yaml
