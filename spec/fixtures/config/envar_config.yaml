---
project_name: '{{PROJECT_NAME}}'
custom_info: '{{PROJECT_AUTHOR}}-{{PROJECT_DATE}}'
reports:
  - uri: '{{REPORT_URI}}'
    format: json
    verbose: true
builds:
  url: "{{BUILD_URL}}"
  service_name: "{{SERVICE_NAME}}"
